`init_database.sql` - intitates the database

`quality_checks.sql` - runs quality checks from bronze and siler layers.

`data analyses.sql` - This is the main SQL file where business questions are answered. 

- SQL files in the folders run data wrangling for bronze, silver, and gold layers. 